{
  "name": "bluesky-discord-bot",
  "version": "1.0.0",
  "description": "Discord bot that monitors Bluesky accounts",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "discord.js": "^14.14.1",
    "@atproto/api": "^0.12.0"
  }
}
```

**2. Create `bot.js`:**

Copy the entire bot code from the artifact into this file.

**3. Create `.gitignore`:**
```
node_modules/
.env
.DS_Store
